#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @d: to be calculated
 * Return: return absolute value
 */
int _abs(int d)
{
	if (d > 0)
	{
		return (d);
	}
	else
	{
		return (-d);
	}
}
